project_name: cartpole_spectral_ortho
sweep_info:
  sampler: grid
  sweep_name: grid_ortho_loss
hyperparams:
  encoder_ortho_loss:
    type: categorical
    options: [0.0001, 0.1]
base_config:
  seed: 1
  encoder_mode: spectral
  domain_name: cartpole
  task_name: swingup
  num_eval_envs: 4
  eval_img_sources: [color, mnist, driving_stereo]
