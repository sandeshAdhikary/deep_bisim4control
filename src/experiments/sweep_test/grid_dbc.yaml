project_name: cartpole_dbc_seeds
sweep_info:
  sampler: grid
  sweep_name: grid_seed
hyperparams:
  seed:
    type: categorical
    options: [1,2,3]
base_config:
  encoder_mode: dbc
  domain_name: cartpole
  task_name: swingup
  num_eval_envs: 4
  eval_img_sources: [color, mnist, driving_stereo]
  img_source: mnist