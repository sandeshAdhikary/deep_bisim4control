project_name: test_16
sweep_info:
  sampler: grid
  sweep_name: test_sweep
hyperparams:
  encoder_ortho_loss:
    type: categorical
    options: [0.0001, 0.1]
  encoder_lr:
    type: categorical
    options: [0.001, 0.0001]
base_config:
  seed: 1
  encoder_mode: spectral
  domain_name: cartpole
  task_name: swingup
  num_eval_envs: 4
  eval_img_sources: [color, mnist, driving_stereo]
  init_steps: 10
  num_train_steps: 1000
  episode_length: 20
  save_model_mode: every_10
  eval_freq: 10
